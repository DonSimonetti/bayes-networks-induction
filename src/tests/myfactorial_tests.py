from src.myFactorial import factorial, generate_factorials, cached_values

# Test
generate_factorials(11)

print(cached_values)
print("min calculated value:", len(cached_values) - 1)
print(factorial(40))
